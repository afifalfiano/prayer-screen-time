name: prayer_screen_time
description: "Prayer times, Qibla compass, and focus mode for Muslims."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.3.4
  geolocator: ^13.0.2
  adhan: ^2.0.0
  flutter_compass: ^0.8.1
  permission_handler: ^11.4.0
  go_router: ^14.8.1
  flutter_local_notifications: ^18.0.1
  workmanager: ^0.9.0
  google_fonts: ^6.2.1
  intl: ^0.20.2
  path_provider: ^2.1.5
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/audio/
